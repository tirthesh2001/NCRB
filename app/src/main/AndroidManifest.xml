<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ncrb" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NCRB" >
        <activity
            android:name=".UserNavActivity"
            android:label="@string/title_activity_user_nav"
            android:theme="@style/Theme.NCRB.NoActionBar" >
        </activity>
        <activity
            android:name=".user_nav"
            android:label="@string/title_activity_user_nav"
            android:theme="@style/Theme.NCRB.NoActionBar" >
        </activity>
        <activity
            android:name=".police_nav"
            android:label="@string/title_activity_police_nav"
            android:theme="@style/Theme.NCRB.NoActionBar" />
        <activity android:name=".Commoner_Login_Page" />
        <activity android:name=".Police_Login_Page" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>